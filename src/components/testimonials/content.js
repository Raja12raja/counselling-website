import React from "react";

export default function Content(props){
    return(
        <span>
            //image of double quotes
            <span>{props.testimonial}</span>
            //ending double quotes image
        </span>
    )
}